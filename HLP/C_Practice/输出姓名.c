#include <stdio.h>

int main()
{
	putchar(getchar() - 32);
	putchar(getchar());
	putchar(getchar());
	putchar(' ');
	putchar(getchar() - 32);
	putchar(getchar());
	putchar(getchar());
	putchar(getchar());
	putchar(getchar());
	putchar(getchar());
	putchar(getchar());
	putchar(getchar());

	putchar('\n');


	return 0;
	
}