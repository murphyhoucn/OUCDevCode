DATA SEGMENT
    ;MEM1 DB 200 DUP(0)
    ;MEM2 DB 200 DUP(1)

    MEM1 DW 200 DUP(0)
    MEM2 DW 200 DUP(0)
DATA ENDS
CODE SEGMENT
ASSUME DS:DATA,CS:CODE

    MOV SI,MEM1
    MOV DI,MEM2

    MOV CX,200

    CLD

    REPE CMPSB

    TEST CX,00FFH

    JZ STOP
    DEC SI
    MOV AL,[SI]
    MOV BX,SI

STOP:
    HLT

CODE ENDS

    END